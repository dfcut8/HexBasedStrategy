Current:
    ☐ Create a basic visual button layout
    ☐ Generate build buttons dynamically. Probably for now just have a list of all available units in some resources...
Future:
    ☐ Refactor everything to not use inheritance and use composition instead.